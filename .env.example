# ================================================================
# Learning Pipeline Hub - Environment Variables
#
# 복사해서 .env 파일로 만든 뒤, 각 환경에 맞게 값을 수정하여 사용하세요.
# cp .env.example .env
# ================================================================

# ---------------------------------
# PostgreSQL 데이터베이스 설정
# ---------------------------------
POSTGRES_USER=analytics
POSTGRES_PASSWORD=analytics
POSTGRES_DB=dwh

# ---------------------------------
# MinIO (S3 호환) 객체 스토리지 설정
# ---------------------------------
# 로컬 파일 시스템 대신 MinIO에 로그를 업로드하려면 true로 설정
USE_MINIO=true

# docker-compose.yml에 정의된 MinIO 서비스 주소
MINIO_ENDPOINT=http://minio:9000

# docker-compose.yml에 정의된 MinIO 접속 정보
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=admin12345

# 로그 파일이 저장될 버킷 이름
MINIO_BUCKET=logs

# ---------------------------------
# FastAPI 웹 서버 설정
# ---------------------------------
# 프론트엔드 개발 서버(Vite)의 CORS 요청을 허용할 주소
# 쉼표로 여러 주소 설정 가능 (예: http://localhost:5173,http://localhost:3000)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:3300

# 트래픽 스파이크 테스트 기본 대상 URL
TRAFFIC_TARGET_URL=http://web:3000/api/events
